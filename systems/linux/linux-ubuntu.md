# Ubuntu

## General

do-release-upgrade do-release-upgrade -d

Actualiza la distribución de Ubuntu

do-release-upgrade -d comprueba si la actualización a la última versión es possible.

do-release-upgrade -m MODE Actualiza a modo server (servidor) o desktop (escritorio).
