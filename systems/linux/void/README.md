# Void Linux

Void Linux setup and operation notes — installation, NetworkManager + DNS strategy, service
management under runit/elogind/turnstile/dinit, NTP and cron daemon choices, NVIDIA configuration,
fonts, and assorted utilities (`xbps-query`, `inotifywait`, global env loading) live as topic files
here; the [`AGENTS.md`](./AGENTS.md) Source Map indexes them file by file.
