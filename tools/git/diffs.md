# Diffs

```sh
# side by side diff
diff -y path/to/file1.txt path/to/file2.txt > side_by_side.diff
# unified diff (single-column default patch format)
diff -u path/to/file1.txt path/to/file2.txt > side_by_side.diff
```
