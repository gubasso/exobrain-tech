# OpenCode

OpenCode's own facts, as dated adapters to the portable rules in
[agent-integration](../../programming/agent-integration/README.md). A rule that holds for any agent
runtime lives there, not here.

- [skills](./skills.md) — the six discovery roots, the specification fields OpenCode reads, and the
  permission patterns that decide whether a skill loads.
